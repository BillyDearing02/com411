# defined function to print the output
def listen(s):
    print(f"That was a loud {s}!")

# gets the user input to be used as
sound = input("What sound did I hear?")
# calls the function and uses the sound variable.
listen(sound)
